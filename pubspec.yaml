name: custom_lint_avoid_null_check_operator
description: A custom lint that warns about null check operator usage.
version: 0.0.1
homepage: https://github.com/tjarvstrand/custom_lint_avoid_null_check_operator

environment:
  sdk: ^3.5.1

# Add regular dependencies here.
dependencies:
  analyzer: ^7.0.0
  analyzer_plugin: ^0.13.0
  custom_lint_builder: ^0.7.5

dev_dependencies:
  flutter_lints: ^5.0.0
  lints: ^5.0.0
  test: ^1.24.0
